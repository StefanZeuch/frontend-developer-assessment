const initialState = {
    API_KEY: "b1f636d93fb132cc82f23792bfe33c3c",
};

export default function (state = initialState, action) {
    var { type } = action;
    switch(type) {
    default:
        return state;
    }
}
